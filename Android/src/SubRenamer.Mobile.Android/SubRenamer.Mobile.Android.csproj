<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-android</TargetFramework>
    <SupportedOSPlatformVersion>23</SupportedOSPlatformVersion>
    <ApplicationId>io.github.subrenamer.mobile</ApplicationId>
    <ApplicationVersion>8</ApplicationVersion>
    <ApplicationDisplayVersion>0.1.7</ApplicationDisplayVersion>
    <AndroidPackageFormat>apk</AndroidPackageFormat>
    <AndroidEnableProfiledAot>false</AndroidEnableProfiledAot>
    <AndroidUseLatestPlatformSdk>true</AndroidUseLatestPlatformSdk>

    <!--
      GitHub Actions produces an APK for sideloading, not an IDE fast-deploy
      payload. Debug builds otherwise default to Fast Deployment and can omit
      managed assemblies from the APK, making a standalone install abort in
      libmonodroid before managed/Avalonia code starts.
    -->
    <EmbedAssembliesIntoApk>true</EmbedAssembliesIntoApk>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Android" Version="$(AvaloniaVersion)" />
    <PackageReference Include="Xamarin.AndroidX.Core.SplashScreen" Version="1.2.0.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SubRenamer.Mobile\SubRenamer.Mobile.csproj" />
  </ItemGroup>
</Project>
